# 企业宣传网站

一个基于 Vue 3 + Vite + Element Plus 构建的现代化企业宣传网站，支持 Netlify 静态部署。

## 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 下一代前端构建工具
- **Element Plus** - 基于 Vue 3 的组件库
- **Vue Router** - 官方路由管理器
- **Netlify** - 静态网站托管平台

## 功能特性

- 🏠 **首页** - 展示企业形象、优势特色、数据统计和客户评价
- 📖 **关于我们** - 公司介绍、使命愿景、核心价值观和发展历程
- 🛍️ **产品服务** - 产品展示、服务流程和服务优势
- 📞 **联系我们** - 联系表单和联系方式展示

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:5173

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

### 预览生产构建

```bash
npm run preview
```

## 部署到 Netlify

### 方法一：通过 Netlify CLI

1. 安装 Netlify CLI：
```bash
npm install -g netlify-cli
```

2. 登录 Netlify：
```bash
netlify login
```

3. 初始化项目：
```bash
netlify init
```

4. 部署：
```bash
netlify deploy --prod
```

### 方法二：通过 Git 仓库

1. 将代码推送到 GitHub/GitLab/Bitbucket
2. 在 Netlify 控制台创建新站点
3. 连接你的 Git 仓库
4. 配置构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
5. 点击部署

### 方法三：拖拽部署

1. 运行 `npm run build` 构建项目
2. 访问 [Netlify Drop](https://app.netlify.com/drop)
3. 将 `dist` 文件夹拖拽到页面中

## 项目结构

```
├── public/              # 静态资源
├── src/
│   ├── components/     # 公共组件
│   │   ├── Header.vue  # 头部导航
│   │   └── Footer.vue  # 页脚
│   ├── views/          # 页面组件
│   │   ├── Home.vue    # 首页
│   │   ├── About.vue   # 关于我们
│   │   ├── Products.vue # 产品服务
│   │   └── Contact.vue  # 联系我们
│   ├── router/         # 路由配置
│   │   └── index.js
│   ├── App.vue         # 根组件
│   └── main.js         # 入口文件
├── index.html          # HTML 模板
├── vite.config.js      # Vite 配置
├── netlify.toml        # Netlify 配置
└── package.json        # 项目配置
```

## 自定义配置

### 修改品牌信息

在各个组件中搜索 "企业品牌" 并替换为你的品牌名称。

### 修改颜色主题

在 `src/main.js` 中可以配置 Element Plus 的主题色，或使用 CSS 变量覆盖默认颜色。

### 添加新页面

1. 在 `src/views/` 创建新组件
2. 在 `src/router/index.js` 添加路由配置
3. 在 `Header.vue` 添加导航链接

## 浏览器支持

现代浏览器和 IE11+（需要 polyfill）

## 许可证

MIT

